fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx[socks]==0.28.1     # [socks]=socksio,支持 SOCKS5 代理(多账号隔离用)
pyyaml==6.0.3
tzdata>=2025.2          # Windows 上为 zoneinfo 提供 IANA 时区数据库
sqlmodel==0.0.22
patchright==1.61.2          # 浏览器自动化(stealth 补丁驱动；shardx-sdk 亦依赖 patchright)
xhshow>=0.2.0          # 小红书请求签名(X-S/X-T/x-S-Common)纯算法库
curl_cffi>=0.7         # 小红书签名直连的 TLS 指纹复刻(impersonate=chrome,绕 JA3 风控)
python-multipart>=0.0.9   # 发布:上传图集/视频文件
openpyxl>=3.1.5          # 监控报告:Excel 导出
opencv-python-headless>=4.8  # 发布:图片尺寸 / 视频封面与元数据
numpy>=1.24               # opencv 依赖
aws-requests-auth>=0.4.3   # 发布:抖音 VOD 上传 AWS V4 签名
requests>=2.31             # 发布:抖音 VOD 上传 + msToken API

websockets>=12
yt-dlp>=2025.1.15        # 通用分享链接解析/下载（抖音、B站、YouTube 等及通用媒体页）
imageio-ffmpeg>=0.5.1    # 内置 ffmpeg 兜底：仅音频提取与分离音视频流合并

# ── 后台管理鉴权(多用户/用户管理/请求审计)──
fastapi-users[sqlalchemy]>=15.0      # 登录/令牌/密码哈希(argon2), DatabaseStrategy 落库可吊销
argon2-cffi>=23.1                    # OWASP 推荐密码哈希(纯 wheel)
slowapi>=0.1.9                       # 登录接口限流(防爆破)
