# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtualenv
.venv/
venv/
env/

# Node (xhs publish signing deps)
node_modules/
_site/

# IDE
.idea/
.vscode/
.worktrees/

# Claude Code 本地技能/配置(不随仓库发布)
.claude/

# Runtime data (login states, downloaded media, sqlite db, uploads)
data/
*.db
config.yaml

# PyInstaller 构建产物
build/
dist/
desktop.log
desktop-error.log

# 内部方案文档(不随公开仓库发布)
docs/

# 独立设备指纹库(已拆分为外部项目 fingerprint-db, 不再入库)
fingerprint-db/

# 抖音 IM 协议调试转储(由代码在运行时写入 bdms/, 属生成物不入库)
app/platforms/douyin/bdms/debug_body.bin
